name: Build Enrollment Service

on:
  push:
    paths:
      - "enrollment-service/**"
  workflow_dispatch:

jobs:
  build-enrollment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push enrollment-service image
        uses: docker/build-push-action@v6
        with:
          context: ./enrollment-service
          file: ./enrollment-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/enrollment-service:latest
            ${{ secrets.DOCKER_USERNAME }}/enrollment-service:${{ github.run_number }}
