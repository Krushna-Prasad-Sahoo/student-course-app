name: Build Student Service

on:
  push:
    paths:
      - "student-service/**"
  workflow_dispatch:

jobs:
  build-student:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push student-service image
        uses: docker/build-push-action@v6
        with:
          context: ./student-service
          file: ./student-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/student-service:latest
            ${{ secrets.DOCKER_USERNAME }}/student-service:${{ github.run_number }}
